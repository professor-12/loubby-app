import Form from "@/components/ui/Form/Form";
import React from "react";

const page = () => {
    return (
        <Form>
            <button type="submit">Hey</button>
        </Form>
    );
};

export default page;
